apply plugin: 'java'

sourceCompatibility = 1.8
version = '1.0'

//repositories {
//    mavenCentral()
//}

repositories {
    maven  {
        url "http://repo1.maven.org/maven2"
    }
}

sourceSets {
    main {
        java {
            srcDir './src'
        }
    }
}

dependencies {
    compile fileTree(dir:'./lib',include:['*.jar'])
}